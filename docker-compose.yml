version: '3.8'

services:
  llm-agent:
    build: .
    container_name: llm_data_agent
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    stdin_open: true
    tty: true
    restart: unless-stopped

