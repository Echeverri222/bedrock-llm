# AWS Configuration
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_REGION=sa-east-1
S3_BUCKET_NAME=your_bucket_name_here

# AWS Bedrock Configuration
BEDROCK_REGION=us-east-1
BEDROCK_MODEL=cohere.command-r-plus-v1:0

# Available Bedrock Models:
# - cohere.command-r-plus-v1:0 (recommended - supports tool use)
# - anthropic.claude-3-5-sonnet-20241022-v2:0 (requires use case approval)
# - anthropic.claude-3-5-haiku-20241022-v1:0 (requires use case approval)

# API Authentication (for FastAPI)
# Generate a secure random token: openssl rand -hex 32
API_TOKEN=your-secret-api-token-here-change-this-in-production

# Optional: Port for API server
PORT=8000

