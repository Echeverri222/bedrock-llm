version: '3.8'

services:
  llm-agent:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: llm_data_agent
    env_file:
      - ../.env
    volumes:
      - ../data:/app/data
      - ../src:/app/src
    stdin_open: true
    tty: true
    restart: unless-stopped

